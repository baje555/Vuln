{
  "name": "vulnlab-tech",
  "version": "1.0.0",
  "description": "Deliberately Vulnerable Web Application for CTF Training",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "bash -c 'chmod +x mvnw && ./mvnw clean package -DskipTests'",
    "start": "java -jar target/vulnlab-tech-1.0.0.jar"
  }
}
